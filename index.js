function changeCompletely(elements, index, array){
  [4, 5, 6].forEach(function(changeCompletely){
      console.log(elements);
  });
} 

function doToElementsInArray(array, callback){
   array.forEach(function(callback){
     console.log(callback);
   });
}

